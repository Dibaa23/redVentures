{
  "name": "dashboard-server",
  "version": "1.0.0",
  "description": "Server for Business Insights Dashboard",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "cd ../dashboard && npm install && npm run build",
    "dev": "nodemon index.js"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}